var a : int = 1;
var b : int = 2;

function sally(z : int) : int {
	b = z;
	return (10);
}

function fred(f : int, g : int) : int {
	sally(f);
	return (g);
}

function main(args : [[int]]) : int {
//	var c : int = 3;
//	var d : int = 4;
	a = fred(5, 6);
//	c = c * c;
//	d = d + d;
	return (0);
}
