var x : {a : int, b : int} ;

function main (argv : [[int]]) : int {
      var x : int;
      var y : int;

      if (x > y | y <= x) {
            return (3);
      }

      return ((x + y) * 3 / x % 11) ;
}
